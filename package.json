{
    "name": "pacoca-io",
    "scripts": {
        "test": "npx hardhat test",
        "build": "npx hardhat compile",
        "node": "npx hardhat node",
        "deploy:local": "npx hardhat run --network localhost scripts/deploy.js > deploy-logs.local.txt",
        "deploy:bsc": "npx hardhat run --network bsc scripts/deploy.js > deploy-logs.txt"
    },
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.2",
        "@nomiclabs/hardhat-waffle": "^2.0.1",
        "chai": "^4.3.4",
        "ethereum-waffle": "^3.3.0",
        "ethers": "^5.1.4",
        "hardhat-abi-exporter": "^2.2.1"
    },
    "dependencies": {
        "@openzeppelin/contracts": "3.4.1",
        "dotenv": "^10.0.0"
    }
}
