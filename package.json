{
    "name": "pacoca-io",
    "scripts": {
        "test": "npx hardhat test",
        "build": "npx hardhat compile",
        "node": "npx hardhat node",
        "deploy": "npx hardhat run --network bsc scripts/deploy.js > deploy-logs.txt",
        "deploy:strat": "npx hardhat run --network bsc scripts/deploy-strat.js",
        "deploy:single": "npx hardhat run --network bsc scripts/deploy-single.js"
    },
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.2",
        "@nomiclabs/hardhat-waffle": "^2.0.1",
        "chai": "^4.3.4",
        "ethereum-waffle": "^3.3.0",
        "ethers": "^5.1.4",
        "hardhat-abi-exporter": "^2.2.1",
        "hardhat-gas-reporter": "^1.0.4"
    },
    "dependencies": {
        "@openzeppelin/contracts": "3.4.1",
        "dotenv": "^10.0.0"
    }
}
